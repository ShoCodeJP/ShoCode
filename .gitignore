# Squad: ignore runtime state (logs, inbox, sessions)
.squad/orchestration-log/
.squad/log/
.squad/decisions/inbox/
.squad/sessions/
# Squad: SubSquad activation file (local to this machine)
.squad-workstream

# Private comms — emails, PII, planning conversations (never commit)
ops/comms/

# Landing page — maintained in ShoCodeJP/ShoCodeJP.github.io (local symlink)
site
